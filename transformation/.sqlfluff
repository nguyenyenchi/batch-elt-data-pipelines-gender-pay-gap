[sqlfluff]
templater = dbt
dialect = postgres
exclude_rules = L016,RF06,LT05

[sqlfluff:templater:dbt]
project_dir = dw
profiles_dir = dw
target = lint

[sqlfluff:templater:jinja]
apply_dbt_builtins = True
